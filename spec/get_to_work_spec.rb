require 'spec_helper'

describe GetToWork do
  it 'has a version number' do
    expect(GetToWork::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
